//import {} from '../actions/data-actions'

  
  const initialState = {
    repair: [],
  }
  
  export const repairReducer = (state = initialState, action) => {
  
    switch (action.type) {
      default: {
        return state;
      }
    }
  }